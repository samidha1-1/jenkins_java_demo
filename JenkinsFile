pipeline {
    agent any
    stages {
        stage('Checkout') { steps { git 'https://github.com/samidha1-1/jenkins_java_demo.git' } }
        stage('Compile') { steps { bat 'javac HelloWorld.java' } }
        stage('Run') { steps { bat 'java HelloWorld' } }
    }
}
